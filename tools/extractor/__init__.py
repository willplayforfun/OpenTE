"""OpenTE asset/data extractor.

Reads a user-owned copy of Trade Empires' `.{}` data files and writes
OpenTE's own JSON tables and PNG sprite assets to a `game_data/` output
directory. See `main.py` for the CLI entry point and
`OpenTE/docs/setup/extracting-game-data.md` for usage.
"""
