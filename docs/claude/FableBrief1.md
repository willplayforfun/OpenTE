There are a few issues Claude instances have been struggling with here. I'm hoping you can identify what has been tripping them up generally, as well as potentially solving the specific issues. You are welcome to read both the high level CLAUDE.md and clone-architecture.md, as well as the `OpenTE/docs/` and the reverse-engineering documentation in `documentation/`. The git history may also help. Perhaps even review the Claude Code session logs (if available), from these sessions: `C:\Users\elevo\AppData\Roaming\Claude\claude-code-sessions\4590db35-d14d-4180-9dcd-2191a5e11e5d\1eb65f64-53df-47c8-b589-8376bda36682`.

I'm hoping there's something about the structure of the docs or the repository that are holding back the Claude instances, and hopefully we can solve that. In general the agents are very resistant to doing deep-dives into the original game code and figuring out the exact behavior to replicate. This is problematic. The agents love using estimations and best-guesses, which doesn't work because (A) they don't know anything about the game, and (B) I'm shooting for a pixel-perfect clone in functionality. The agents also keep trying to use their image-interpretation tools on the game sprites (which doesn't really work at all) and the simulated renderings from test beds (which kind of works but has so many false positives and negatives that it is barely worth it). Yet, surely, the entirety of the game's functionality can be understood from the compiled code and the data -- that's all there is! 

Now, on to the specific issues we've been struggling with. We just got trails and roads rendering working pretty well, but canals and railroads continue to be a problem. They aren't rendering the proper tiles from the atlases. In addition, there is no functionality in OpenTE for bridges right now, but that's a big part of roads -- they will automatically create bridges cross water when dragged across bodies of water. I also worry the existing trail/road code won't elegantly allow for that to be implemented without introducing lots of bugs.

Generally I worry the quality of the code and the commenting in the code (in terms of style, being verbose, not being actually helpful comments, persisting certain fixations on long-solved problems, etc.) is holding the lower-level agents back.

So, I'd like you to take a gander at the project gestalt and provide your insights, as well as fix the current issues with the canals/railroads/bridges, or at least set things up so a lesser agent can succeed in a straightforward manner.